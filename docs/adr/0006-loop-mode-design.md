# ADR-0006: Loop mode requires a separate design decision

**Status**: proposed  
**Date**: 2026-07-12

## Decision

Do not implement loop mode until its budget, authorization, retry, and lifecycle rules are approved. `loopMode` remains a disabled feature flag.
