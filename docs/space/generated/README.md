# 生成材料边界

状态：generated
最后更新：2026-04-27

`docs/space/generated/` 下的材料是派生内容。

规则：

- 生成内容的权威性低于 `execution/active/`。
- 生成内容不能静默覆盖 active 执行规则。
- 如果生成材料提出新规则，必须带着理由和测试手动晋升到 `execution/active/`。
- 如果生成材料总结外部系统，必须引用或命名上游来源，并明确标记为派生摘要。

该目录用于草稿、综合对比和临时摘要。它们可以有用，但不能因为存在于仓库中就自动变成约束。
