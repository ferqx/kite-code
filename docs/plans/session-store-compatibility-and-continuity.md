# 会话存储兼容性与连续性：V1 最终实施方案

状态：本次约定范围已完成，待用户决定是否提交。用户于 2026-09-17 明确暂不要求 Windows 验收；第 24 节为最终交付结论。Windows 未实测，不计为通过。

日期：2026-09-17。适用：Desktop、TUI、CLI、配套 Service，以及共享同一 Store 的 daemon/Web 读取入口。

本方案取代“版本不兼容时自动切换空 Profile”的处理方向，保留已完成的安全拒绝、有限启动诊断和数据不变验证。用户已另行授权按本方案实施。实际数据收敛必须在隔离演练、维护准入和发布验证全部完成后执行。

## 1. 产品承诺与边界

**正常使用访问唯一的正式会话数据源。对声明支持的版本变化，程序自动完成兼容或无损转换，保持会话身份、历史、关联和可发现性。不能安全处理时保留当前数据，提供可操作的恢复入口，不切换空库、不丢弃未知数据、不恢复过时备份来冒充兼容成功。**

“唯一”约束的是正式数据源、访问入口和写入权威，不禁止 SQLite 自有 WAL/SHM/事务日志或受控备份。备份不参与正常查询，不接受新写入，不自动成为正式库。显式自动化测试可以使用临时隔离数据；普通开发启动、版本切换和不同客户端不能静默创建另一份用户数据。

数据唯一不等于只有一个 Service 进程：保留现有不同会话可由不同合法 owner 执行的模型，不借本次设计重新引入全局执行 writer。

三项能力分别验收：

| 能力 | 必须满足 |
| --- | --- |
| 读取历史 | 原会话可发现、可打开；内容和关联未丢失；读取失败不能伪装成空历史 |
| 原会话开始新一轮对话 | 沿用原 sessionId；通过当前授权和执行检查后追加消息，不新建替代会话 |
| 恢复未完成 Run | 按持久事实、执行所有权和外部结果确定；不承诺所有旧任务自动续跑 |

不承诺任意已发布旧程序能够理解未来格式，不把“安全拒绝”计为该路径兼容成功。不新增通用迁移平台、长期双写、第二套会话索引、后台格式转换任务或任意数据库自动修复工具。

## 2. 当前事实与缺口

以下来自本次工作树与先前只读审计，不是目标实现：

- 当前 Runtime：schema 10，epoch `kite-session-app-server-2026-09-02`。
- 本机旧 profile：schema 11，epoch `kite-session-accepted-runs-2026-09-15`。相对当前结构，runtime_runs 增加 input_json、preparation_failure_count，调整状态/时间约束并增加待处理输入索引。
- 当前 reader/writer 不维护上述新增 Run 语义；可达 Git 历史未找到 schema 11 的完整实现。当前数据未使用新增字段的先前快照，不等于此后永远不用，更不构成通用降级依据。
- 现有 Store 打开入口先只读预检，支持完整当前结构；已有结构化启动错误、快速退出诊断和正常退出处理。
- 上一轮 epoch source profile 分流让开发程序进入另一份 schema 10 数据。这证明能启动，不能证明旧历史连续。
- 当前文档中的旧 kite.sqlite“保留但不可见”也是需要明确处置的历史入口，不能直接标记为已迁移。
- 已有新建、当前格式重启、拒绝不兼容且字节不变的真实 Desktop 测试；尚无支持版本之间的完整会话守恒证明。

必须补齐：统一数据入口；明确实际支持的格式与路径；跨进程维护协调；经过审计的转换与会话保留验证；启动前恢复交互；Desktop/TUI 跨版本 E2E。

## 3. 格式身份与支持矩阵

### 3.1 分开四种兼容性

应用版本不是 Store schema。数据库结构可读不等于事件/State 可解码，历史可读不等于 writer 可写，Store 可写也不等于客户端和 Service 协议兼容。

保留已有 format_epoch 持久值；不为了统一名字批量重写数据库身份。schema 表示结构演进，epoch 仅表示无法沿用原语义的边界。两者都不能单独触发新建 Profile。

兼容判断必须覆盖：表/索引/约束、State/event schema、Run 状态与输入、receipt 和 Effect 幂等、执行 fencing、Workspace/Fork 关系、Artifact 编码及引用。用户授权不能作为“强制忽略未知格式”的开关。

### 3.2 V1 决策表

| 组合 | 处理 | 是否可以宣称已支持 |
| --- | --- | --- |
| 已验证的原格式读写实现 | 原库直接打开 | 完整 E2E 通过后可以 |
| 已验证来源到目标的无损升级 | 自动进入受控迁移 | 迁移、故障注入和连续性验证通过后可以 |
| 较新格式被较旧 Runtime 打开 | 优先寻找已实现的安全读写能力；其次仅使用经过验证的无损逆转换 | 没有路径就不能声明支持降级 |
| 仅能可靠读取历史 | 只有隔离所有写入口的真实只读组合才允许进入 | V1 不为未证明路径搭建通用只读 Runtime |
| 未知 epoch/schema 或未知事件语义 | 不写入；进入恢复界面 | 不兼容边界通过，不等于兼容能力通过 |
| 无法读取元数据、损坏、权限不足 | 分别诊断，不误标为“只需迁移” | 不尝试用 DDL 修复物理故障 |

支持关系用当前 owner 内少量显式分支/转换函数表达；每条转换有实际源 fixture、明确目标及验证器。不要用 `schema <= current` 或未经证明的 min/max 范围。`migration_required` 只表示存在已实现、可执行的路径；较旧数字本身不证明存在迁移路径。

### 3.3 本机 schema 10／11 的实施决策

先取证 schema 11 的代码/制品来源，并对当前完整 Store 做隔离副本审计。优先补齐目标 Runtime 对确有需求的较新数据语义，再为 schema 10 提供向前转换，避免围绕旧代码删减数据。

不得现在指定“把所有 11 降成 10”，也不得只改 schema/epoch 常量。若无法取得来源，转换只能依据已充分验证的实际布局和事件语义，支持范围明确限定；无法证明的记录必须阻断转换，不能跳过。源格式审计未完成是该路径的实施门禁，其他入口和诊断工作可以独立推进。

旧 kite.sqlite 若确认包含未删除用户会话，也必须列入实际数据处置范围：有明确转换路径才纳入当前库；否则明确报告仍未可访问的历史，本机连续性目标不能标记完成。

## 4. 唯一正式数据入口

### 4.1 正常解析规则

同一 OS 用户的同一 canonical config root，Desktop、TUI、CLI、daemon 使用同一正式 Store 路径；默认路径为该 root 下的 kite-session.sqlite。已有显式数据根选择保留，但必须清楚标明这是另一份用户选择的数据，不能由版本或错误回退隐式触发。

source/installed 的代码位置不决定用户数据位置。撤回 checkout/epoch 自动分流；启动前发现已知历史位置尚未收敛时，不创建空正式库并展示正常首页。无历史位置且确属首次使用时才初始化新库。发现范围限于既有明确路径，不扫描整个 home。

使用既有 canonical/no-follow/owner 检查；持久事实不能仅凭目录名称判断。所有客户端使用同一个 resolver；没有必要不新增 profile registry 或长期 locator 数据库。

### 4.2 已有多目录的一次性收敛

这不是日常迁移分支。先停止相关派发，明确全部候选库的身份与最终提交点，并核对外置资产。

1. 两边一份为空：保留非空数据及其身份，在维护保护下发布至唯一正式位置；空库在验证后才清理。
2. 一份是另一份的完整、可证明子集：以内容与因果关系验证为准，选择保留全部事实的目标；不能只比较条数或修改时间。
3. 两边都有独立新增会话：只有明确的、有 fixture 的合并规则才可转换；sessionId 不重写，所有关联、回执、删除标记和 authority 语义一并核对。
4. 相同身份出现分叉、冲突或来源不明：停止自动合并并报告冲突范围。不能按更新时间覆盖，不能擅自替用户选择丢弃哪份历史。

不要为一般用户新建通用多库合并框架。若当前实际数据不存在第三、四类，实施到有证据需要的范围即可；如果存在，它就是本机收敛的真实阻塞，不能隐去。

目标发布须可恢复：事务内验证目标，目标关闭并确认持久化后才切换入口；跨文件系统不假设 rename 原子。崩溃后能区分已发布目标与未完成暂存，不能重复导入。旧位置在完整验证前不删除，发布后退出所有正常 resolver；保留副本仅作为明确标记的恢复资产。旧程序若仍可写旧位置，则收敛尚未成立，须先解决旧入口准入。

## 5. 迁移所有权与并发

迁移由共享 Service/Store owner 执行，Desktop/TUI 展示进度和恢复操作，不包含 SQL 转换。Kernel 不获得 I/O 或迁移责任。

一个 Session 的执行租约不能代替整个 Store 的维护权限，SQLite 写事务也不能保证一个旧程序在迁移提交后不继续按旧语义写入。

实施要求：所有受支持 Service 在打开 Store 前进入同一维护准入；正常连接持有共享准入，转换需要独占准入直到验证结束、数据库关闭或重新开放。先复用经过证明适用的现有本地锁机制；若不足，新增一个以 canonical 数据源为键的最小跨进程门闩，不能做成协调服务。锁载体不能因删除/重建产生两把锁；OS 崩溃释放行为须实测。原定 Windows/POSIX 平台要求中，Windows 验收依第24节用户指示暂缓，不作为本次完成门禁，也不计为已通过。

转换前停止新写入和派发，等待已开始操作到达可持久化边界。超时或外部结果未知时进入等待/受限恢复，不伪造清理完成，不强杀无明确归属的进程。只读历史连接也需在 DDL 边界正确关闭或协调。

已持久化的 unknown/waiting 本身不是所有结构迁移的禁令；若转换可以原样保留其事实且没有仍在执行的旧 owner，可转换历史并继续限制该 Run 的执行。不能把要求“外部结果全部已知”变成所有会话历史升级的全局门禁。

未接入维护协议的旧 Service 不能被新锁自动保护。必须证明它已退出，并通过受支持启动入口阻止在迁移期间重新进入；无法证明时不开始迁移。PID 文件、端口空闲或 SQLite 当前没被锁住，都不能单独证明这一点。任意手工启动、绕过准入的历史二进制不纳入跨版本并发兼容承诺。

并发启动只允许一个迁移者；其他客户端显示等待结果，迁移结束后重新只读识别版本，不重复执行转换。

## 6. 自动迁移流程与崩溃恢复

自动只适用于已公布且通过验证的无损路径；不是任意启动读查询的副作用。启动探测、历史查询与 status 保持只读，由专门的启动准备阶段执行转换。

顺序固定为：

1. 只读识别数据源、格式、可执行路径及所需空间；区分权限、锁冲突、损坏、未知格式。
2. 取得维护权；停止相关写入与派发、收尾；再次核实文件身份、格式与版本，防止使用过期探测结果。
3. 从最终已提交快照生成会话保留基线；通过 SQLite 一致性备份机制创建并验证恢复副本，不只复制活跃 WAL 主文件。备份持有时间点、源/目标格式、摘要和受控资产清单；验证副本可独立打开，必要的 journal 规范化只发生在副本，绝不误作用到源。空间不足、备份失败或资产未准备好时不改原库。
4. 在受维护保护的私有候选 Store 事务内执行明确转换。DDL、数据转换及 schema/epoch 更新一起提交；不要提前改版本号。完成验证后按下述可恢复文件发布规则替换唯一正式入口；候选不接受正常客户端读写，也不是第二份正式数据库。
5. 提交前执行结构、外键、转换语义和会话保留检查。任一失败整体回滚，不按会话跳过。
6. 提交后仍保持维护权，使用目标 reader 重新打开并做应用层验证；成功后才向正常客户端开放。
7. 释放维护权，重新读取目录与原会话，按当前授权开放新一轮对话；旧 Run 另行进入执行恢复判定。

优先单次事务完成实际路径，不实现分批迁移引擎。事务外文件操作不得冒充 SQLite 事务原子性；涉及外置资产时先准备不可变副本并校验、再提交引用，原文件保留至验证结束。确需可恢复文件发布时使用最少的受保护阶段记录，记录只用于该转换，不能成为第二份业务状态。

| 中断点 | 重启后的正确行为 |
| --- | --- |
| 备份前/备份中 | 原库未改；不把未完成备份当作可恢复点 |
| 事务开始后、提交前 | 由 SQLite 恢复事务；重新识别源格式后按同一路径处理 |
| 提交结果未知 | 读取实际 schema 与转换后不变量，不依据进程退出码猜测，不再次盲目转换 |
| 已提交、开放客户端前验证失败 | 保留目标数据和备份，进入维护恢复；不能自动覆盖回旧备份 |
| 已开放且产生新消息后出错 | 保留当前最新数据；重装程序不恢复旧数据快照 |

禁止关闭 journaling/synchronous 来通过性能测试，禁止边迁移边开放写入。具体耗时随数据量显示阶段与活动状态，不承诺固定秒数，也不每次普通启动扫描全部历史。

## 7. 会话保留基线与验证

基线是此次转换的验证输入，在最终一致快照计算；不新增永久会话台账。数量是辅助指标，不能替代集合、内容和关系校验。

| 对象 | 验证要求 |
| --- | --- |
| Session | 未删除 sessionId 集合相同；不替换身份；名称与必要元数据语义保留 |
| 删除事实 | 不复活 tombstone 指向的会话，不误删现存会话；保留相关去重语义 |
| 历史/Event | 身份、顺序、内容、因果和 revision 边界等价；纯结构转换尽量保持原业务字节 |
| Workspace/Fork | 持久归属与来源关系保留；路径变化不重建身份或自动扩大授权 |
| Run/receipt/Effect | 完成不变待执行、未知不变成功；commandId、幂等、顺序与关联保留 |
| Snapshot | 可解码并对应正确历史边界；派生快照仅在有完整可重放历史且已验证重建路径时重建 |
| Artifact/附件 | 内容可读、引用可解析；资产清单覆盖真实外置位置，不能只备份 SQLite 后宣称全部资产已备份 |
| 执行权/恢复 | 保留持久 fencing 和执行事实，不把旧租约复制成新进程有效执行权；恢复沿现有规则重新判定 |
| 客户端投影 | 在同一授权范围内列表可发现、历史可打开；可继续者向原 sessionId 追加 |

语义转换用转换专属验证器证明等价，不能要求所有转换前后字节一致，也不能把“字段被丢弃”作为正常等价。已有损坏或缺失资产需记录原有缺陷；禁止迁移制造新缺失，禁止静默跳过无法验证的保留对象。必要时整体停止并提供诊断。

草稿和未落盘流式片段单独处理：客户端升级关闭前保存已支持的草稿；外部安装器无法触达的运行实例不能被承诺自动保存。明确区分已提交数据、未发送草稿和未知执行结果。

## 8. 列表、历史和执行恢复

Store 不支持时客户端显示存储恢复界面，不渲染“没有会话”的成功态。历史目录接口不得 catch 后丢弃坏记录。

Store 可读但个别会话解码失败时：可靠确认身份与授权后，在既有会话摘要中保留错误入口；不能确认时返回明确的部分加载错误与可重试状态，不暴露未知会话信息。分页必须传播不完整状态，不能把失败页当作空尾页。尽量沿用现有摘要与错误响应，不新建第二份索引。

Workspace 不可达或授权失效时，允许在原有安全范围内读取历史，并提示关联或授权步骤；这不授予工具执行权限。重新认证、选模型与修复 Workspace 不替换 sessionId。

新一轮对话遵循已有 Run 排序和恢复约束；历史可读不代表可以越过未知旧 Effect 直接派发。对未完成 Run 分类为可按既有规则继续、需要用户决定、外部结果未知或暂不支持。迁移不自动批准权限，不重放已完成工具，不写虚假的 waiver、receipt 或 completion。

## 9. 启动交互与恢复出口

Desktop 与 TUI 使用同一套错误原因与处理能力，界面分别呈现；使用现有启动控制链路，无需为了迁移先成功初始化正常业务 Runtime。

结构化诊断最少包含受限错误码、迁移阶段、实际/目标格式版本、是否可重试及当前可执行操作。不要返回任意 stderr、凭据或整条数据库记录。原始错误仅进入受保护诊断；用户界面使用明确说明。

| 状态 | 用户看到什么 | 程序实际能做什么 |
| --- | --- | --- |
| 正在安全转换 | 阶段、已有数据正在保留、取消限制 | 提交前可安全中止；提交期间等待结算，不杀进程冒充取消 |
| 有其他客户端使用 | 哪类实例正在占用及等待状态 | 对自身可确认实例正常收尾；重试准入，不强停外部任务 |
| 空间/权限问题 | 具体原因与所需条件 | 保留原库，条件修复后重试，不切空目录 |
| 未知格式/没有无损路径 | 会话数据仍在，此程序暂不能打开 | 重新检查、保存脱敏诊断、通过已有可靠安装渠道使用兼容版本 |
| 个别记录问题 | 部分加载/原会话读取失败 | 保留入口与原数据；有专属已验证修复器才自动修复 |

不能显示不可执行的“自动修复”按钮。没有可靠更新设施时，不在 V1 临时新增自动下载、任意远端脚本或静默替换程序机制；提供明确的安装/恢复指引和再次打开操作。受支持路径仍必须自动处理，未知路径则允许安全停止，不能把用户无从操作的一句错误当完成。

更新程序版本与恢复旧备份是两件事。重新使用兼容程序应继续打开当前最新 Store，不恢复旧快照。时间点恢复会丢失该时间点后的可见记录，必须明确范围、由用户选择且保留当前最新副本，不能自动覆盖唯一数据。

## 10. 实施归属与最小改动

| Owner/入口 | 工作 |
| --- | --- |
| runtime-storage-sqlite | 元数据/结构判定、实际转换函数、事务、备份与会话保留校验；细分损坏/权限/不支持 |
| kite-local-runtime / release composition | 唯一数据入口、启动准备协调；复用本地生命周期能力，不负责业务 SQL |
| kite-service | 启动前维护协调与存储准备；确保正常执行端口在验证前不开放 |
| Desktop main/preload + TUI/CLI 启动层 | 一致的准备状态、等待、取消和恢复说明；业务 renderer 不取得文件权限 |
| Runtime history/read projection | 不静默丢记录、部分加载状态、原身份续聊与恢复边界 |
| release/tests | 真实版本制品与脱敏 fixture、故障注入、跨客户端降升级验证 |

现有重点入口包括 kite-session-runtime-file.ts、kite-session-runtime-storage.ts、kite-home-store.ts、现有 run/state reader 与 migration 工具、Service app-server.ts、Desktop host.ts/service-process.ts、release/local-service-client.ts、local-runtime/source-profile.ts。先审查已有 migration.ts/run-migration.ts 的真实适用范围，不因名称相同直接复用，也不复制成第二套机制。

删除自动 epoch 分流时同步 CLI、Desktop build 注入和测试，保留结构化启动诊断。已有 same-build 协议配对不因数据库兼容而放宽。

## 11. 实施阶段与验收门禁

| 阶段 | 产出与完成条件 | 依赖 |
| --- | --- | --- |
| P0 事实与路径审计 | 格式来源、全部实际数据位置、资产位置、源/目标语义、支持矩阵与非支持边界；确定唯一正式入口 | 无 |
| P1 入口连续与恢复交互 | 撤回静默 profile 分流；已知旧库存在时不空建；Desktop/TUI 显示真实状态；多目录处置有可恢复方案 | P0 |
| P2 维护协调与迁移安全边界 | 受支持进程准入、备份、空间与身份复核、并发/中断测试；旧进程保护有证据 | P0 |
| P3 实际转换与会话保留 | 为真实所需格式逐条实现并验证；目标 reader/writer 能理解转换结果；不添加无消费者转换 | P2、源格式审计 |
| P4 端到端连续性 | 原身份历史、续聊、恢复边界、混合客户端与跨版本矩阵通过 | P1–P3 |
| P5 本机数据收敛与交付 | 在真实数据隔离副本演练后处理本机；唯一入口、旧历史可访问、新增记录保留；文档和最终 review 通过 | P4 |

禁止 P1 仅做到“新库可启动”就宣布完成。源格式来源不足、冲突数据无法无损合并、旧进程无法可靠协调，都应明确阻塞对应阶段；不得通过忽略记录降低验收标准。

每阶段同步现有 owner/active/产品文档并执行过度设计检查。可以并行做只读审计和独立客户端测试，Store authority 与迁移提交由一个 owner 串行集成。

## 12. 必须通过的测试矩阵

| 编号 | 场景与关键断言 |
| --- | --- |
| C01 | 新用户无历史入口：只创建一个正式库；所有正常客户端解析同一路径 |
| C02 | 已知历史库存在、正式位置缺失：不生成空首页；完成有据转换/收敛或明确进入恢复流程 |
| C03 | 实际受支持旧版本创建数据，再升级：原 ID 集合、历史、归属及关联等价 |
| C04 | 升级后在原会话继续：追加到原 sessionId；不创建替代 Session |
| C05 | Fork、工具结果、快照、Artifact、tombstone、receipt、Run、外置资产保留；未知执行不重复派发 |
| C06 | 每个迁移提交边界故障注入，含备份失败、磁盘满、事务中断和提交结果未知：无部分转换、无重复导入、无空库回退 |
| C07 | 个别 State/Event 无法解码和分页读取失败：入口或部分错误可见，不伪装完整成功 |
| C08 | 旧版本 N 条→升级仍 N 条→新增 M 条→不兼容降级→回到受支持版本：原 N+M 条按身份和内容全部存在；不回滚到旧备份 |
| C09 | Desktop/TUI 不同受支持版本同时打开：无并发格式往返、单一迁移者、旧 writer 不在迁移后继续写 |
| C10 | Workspace 路径/权限改变：历史不静默消失，执行不扩大授权；必要重新关联保持身份 |
| C11 | 未知未来 schema/epoch：原数据和关联资产不变；显示可执行恢复入口；重新使用兼容版本不丢新增数据 |
| C12 | 当前本机 schema10/11、多profile及 kite.sqlite 实际处置：无遗漏历史、无ID替换，最终唯一正常访问入口 |
| C13 | 取消迁移/关闭客户端、多个启动者、重启后恢复：状态有界、无残留错误维护权、无重复执行 |
| C14 | 草稿/未提交流式内容：按各客户端独立承诺测试，不混入“数据库已保存”结论 |

C03–C09 必须使用实际版本制品或能准确复现该版本读写与事件语义的脱敏 fixture。只改 meta.schema_version 的 synthetic 数据只能测错误分类，不能证明真实跨版本迁移。Desktop 走真实 packaged Electron/Service，TUI 走真实入口与 PTY；两者复用相同数据保留断言。

验证层次：结构和元数据 → SQL/事务与备份 → 目标 State/event 解码 → Service history/command → 客户端可见性与原会话续聊。Linux/Windows 的未完成资格必须明确，不能拿 macOS 结果替代。

实施后的仓库门禁：相关 Store/Host/Service/client 测试、全工作区 typecheck、format:check、core/runtime/API boundary、test-ownership、check:docs、all 作用域 check:docs-impact；发布入口与证据变化另跑相关 release/plan-evidence 检查。通过项必须对应最终集成版本。

## 13. 发布与完成判定

发布前写明受支持来源/目标格式及最低参与维护协议版本。schema writer、新 reader、迁移器与维护门闩必须在同一合格制品中交付，不能先放出写新格式的版本再补旧数据处理。

程序回滚不自动回滚数据库。已有历史二进制不能被新代码追溯改造；若它不具备未来格式安全拒绝或维护协议，须明确排除其混跑，并在受支持安装入口拦截该组合。

只有以下条件全部成立，才能标记本方案实施完成：

- 唯一正式入口落实，版本变化不会生成另一份正常用户库。
- 实际承诺的路径有实现与验证，未知路径不破坏数据且提供可操作恢复说明。
- 原身份、历史、关联、发现与打开、原会话续聊均通过验收；执行恢复独立且不伪造事实。
- 已知真实历史数据的位置和可访问性已交代，不能以“文件还在”代替完成。
- 故障中断、混合客户端和降级后再升级通过，不丢升级后的新增记录。
- 文档与制品一致，未支持路径和平台明确，不以启动烟测代替迁移资格。

本次文档交付的完成仅指方案与审查，不指上述代码或本机数据处理已经完成。

## 14. 设计审查与资料

过度设计审查：唯一正式入口、跨进程维护、实际转换、会话保留验证和客户端恢复说明为 required；自动 epoch 分流及仅按数字推断可迁移应在实施中移除；通用迁移图、任意版本逆迁移、通用多库合并、自动下载更新器和长期迁移台账为 defer，不搭建空框架。设计结论：按上述边界实施。

SQLite 事务用于原子提交，不能替代业务语义验证；一致性备份是恢复资产，不证明升级后会话可见。参考：[原子提交](https://www.sqlite.org/atomiccommit.html)、[Online Backup API](https://www.sqlite.org/backup.html)、[WAL 与附属文件](https://www.sqlite.org/wal.html)。

设计交付核对：已对当前 Store 打开、source profile、Service 启动与相关产品文档核实；完成独立审查。`document-before-commit(action=design_complete)` 为 ready，`overengineering-check` 为 pass；文档结构、all 作用域文档影响检查和 diff whitespace 检查通过。本轮仅文档变化，不以既有测试结果替代尚未实现的迁移验证。


## 15. 实施进度（2026-09-17）

状态：实施中，未达到第 13 节完成条件。此前能力门禁与启动诊断代码已提交为 `47c5e4f1`，本节描述其后的未提交实施进度。

已落地：source/installed 的 Desktop/TUI/CLI 正常入口统一；历史来源只读发现并阻止空库替代；Store owner 共享维护锁与独占维护 primitive；首次初始化的白名单诊断在 Desktop/TUI/CLI 透传；旧 journal 列表不再吞掉事件解码错误。维护锁不能单独替代停止旧 writer 和迁移事务。

实际数据审计发现四份独立非空来源，Session ID 无两两重叠：正式 schema10 有 1 条会话、历史 source schema10 有 1 条、source schema11 有 121 条、旧 kite.sqlite schema9 有 62 条。原文件保持不变。使用只读 SQLite backup 的隔离副本运行当前生产 State/Event codec 与 Artifact reader：360 条 State、33,593 条 Event、5,890 条 Artifact 的读取核验失败数为零。副本已清理。该结果仅证明现有记录的读取，不证明全部引用关系、Run/receipt/authority 语义等价或可安全写入。

尚未解决的关键证据：schema11 `kite-session-accepted-runs-2026-09-15` 的实际 writer 实现未从当前 refs、reflog、可恢复对象及现存工作树找到；已向用户询问来源版本、提交或制品。不得用 DDL 与解码成功推断可以删列或修改版本号。schema9/10 的 authority/effect 语义差异也必须有专属转换证明。

尚未完成：schema11专属转换、将schema9转换与一致性备份接入维护期事务编排、多来源 Workspace/authority/Run 关联收敛、自动恢复交互、完整跨版本 E2E 及本机历史数据收敛。schema9离线转换及schema10恢复备份的进展见第16节。当前遇到未归并历史会明确阻止正常启动，属于保护性中间态，尚不满足“由客户端自动处理兼容问题”的最终要求。Windows 锁已有实现，但无真实 Windows runner 资格证据。


已补充 schema10 保留清单：单一读取事务内校验精确表结构及 quick/FK，遍历全部表/列并按主键流式计算类型明确的摘要，保留 int64 精度。相同数量但替换会话身份、修改历史正文或关联仍会失败。当前只有专属测试消费者，属于尚未接入迁移的进行中代码；不将其称为跨格式转换证明。

本次集成验证：7 个相关文件共 56 项测试通过、1 项 Windows 专用测试跳过；此后新增真实 Service 历史来源测试，App Server 进程测试 8 项全部通过；schema10 保留核对 6 项通过。Desktop 配套 Service smoke、TUI startup 与 session-persistence 两个真实 PTY 场景通过，均使用隔离 Home。全工作区 typecheck、format:check、core/runtime/API 边界、测试归属、文档结构、all 作用域文档影响和 plan-evidence 检查通过；format 仍报告 8 条既有 informational 提示。上述结果不覆盖跨 schema 升降级、Windows 真机或实际历史归并。

阶段审查：唯一入口、启动失败透传、历史来源防空库替代与维护锁为 required；没有新增迁移图、版本下载器或长期会话台账。保留验证器是当前迁移任务的未接入部分，不能单独宣称阶段完成。实际迁移阶段仍保持 in_progress，现有历史来源检查不提供旧 writer 隔离或一致性快照，后续收敛必须先取得这些证据。


## 16. 已知格式路径的进一步实施

真实兼容路径已补证：从提交 `47c5e4f16c6675cb99712a696abd8a5e485abe27` 独立归档源码，离线安装锁定依赖，核验旧 workspace 依赖均解析在该归档中。旧 Service/Client 在隔离 canonical Home 创建会话并回复；当前 Service/Client 以原 ID 续聊并新建另一条含回复的会话；再次用旧版和当前版打开，核验两条 ID、用户消息与助手回复的类型、内容和顺序。可复跑入口为 `tests/qualification/session-store-schema10-cross-version.ts <old-source-root> [current-source-root]`。这证明这两个具体 schema10/相同 epoch 实现的往返读写，不证明任意旧版、默认旧 source profile 入口、并发混跑或9/11转换。

schema9审查进一步明确：旧 `releaseControl` 只写 idle/normal，不验证执行清理。实际旧源35条idle中仅11条通过现有 State迁移就绪检查，不能把其余24条自动视为可执行。转换子步骤需复用旧版完整 snapshot 读取校验，在旧writer已停止的外部前提下，只对旧idle/normal且持久状态就绪、无lease/effect/resource未结证据的会话建立新idle；其他会话保留原事实并进入recovery_required。这不等于已完成真实恢复或原会话续聊验收。

schema10恢复备份使用私有恢复目录内的 SQLite `VACUUM INTO` 一致快照，包含WAL已提交数据；复用逐表内容清单验证副本，副本转成可独立读取的journal模式，完成持久化后才写完成清单。源不执行journal切换、不覆盖。恢复副本只作恢复资产，不加入正常数据入口。此方法的快照性质见[SQLite VACUUM INTO](https://www.sqlite.org/lang_vacuum.html#vacuum_with_an_into_clause)。当前实现限制为已知schema10，尚未组成自动迁移流程或处理用户实际数据，Windows恢复备份仍未完成真机资格。


schema9专属转换子步骤已实现，使用旧生产写入器生成fixture，9项测试覆盖原身份/历史/Run保留、保守执行权映射、未知元数据或触发器拒绝、损坏快照拒绝及目标DDL后的整事务回滚。进一步对真实旧库的只读一致隔离副本执行转换：62条会话、24条Run、1,046条事件均保留；当前生产读取器成功读取62条快照、1,046条strict events、62条执行权及24条Run，失败数为零，51条会话仍需恢复核验。实际源main/WAL前后摘要相等，副本已清理。此结果不能替代旧writer停写证明、原位发布及客户端自动恢复验收。

schema10备份6项测试通过，包含WAL最新提交、无sidecar的已关闭WAL库、副本完成后的fsync/完成清单失败、目录碰撞不删除已有资产。备份完成标记仅在副本独立校验和持久化后发布。保留清单6项测试继续通过，本轮三组共21项。全工作区类型检查与Storage最终类型检查通过。

schema11新增审查事实：191条Run的input_json全NULL、preparation_failure_count全0，均有started_at和匹配start receipt；此外capability_artifacts的唯一约束也与10不同（11仅invocation_id唯一，10为invocation_id与evidence_digest组合唯一）。12条running Run中有1条所属authority为idle，不能把authority文字当作可恢复证明。来源代码未找到不构成逻辑上的永久禁令；但仅凭这些聚合也不足以放行。下一步仍须完整生产parser、State/journal/dispatch及目标写入守恒验证，支持范围只能限定到被证明的实际语义子集；尚未实现或执行11转换。


本轮审查结论：一致性备份、专属转换、逐表内容验证及真实旧源码往返测试均由已接受需求直接要求；未加入通用迁移图、隐式版本下载或自动恢复旧备份。备份/转换尚无启动准备阶段的生产调用，整体阶段继续保持in_progress，不以工具函数与其单测宣称已交付自动兼容。文档同步与相关验证完成，完整目标仍未完成。


## 17. 启动准备与四源收敛集成（2026-09-17）

本节更新第15、16节的阶段状态，不将先前的“尚未实现”沿用为当前判断。实施仍未完整验收；本机原数据已按下述已验证路径完成转换、收敛与归档。

当前实现：Service默认Store组合入口在打开正常连接前运行单次准备。普通当前格式启动只检查精确格式和已知入口；发现已知历史源或严格限定的11格式才进入独占维护。所有源与canonical锁持续覆盖一致性备份、私有副本转换、合并、目标读取校验和可恢复发布，备份借用外层锁而不提前释放。原数据不作为转换工作副本。

支持的转换子集：9采用生产旧布局及已验证的旧authority映射，非空旧Effect等未证明事实拒绝；11核对完整DDL、epoch、所有Run输入为空/准备失败计数为零及可表达的Run/receipt等约束，非该子集拒绝。旧执行权的失效只进入recovery_required，不伪造cleanup、success或waiver。10来源合并仅接受精确身份相同的重复事实；共享Workspace仅合并创建/更新时间边界，其余冲突拒绝。所有来源纳入验证，0字节main伴随WAL/SHM不能被跳过后归档。

发布只使用一个固定短期意图文件。未完成意图留在私有迁移资产内，完整写入、fsync、rename至正式意图并同步目录后才移动原文件。旧main/WAL/SHM逐项归档；候选在同文件系统发布至唯一正式位置。启动首先恢复未结清意图，逐文件核对指纹，不重新导入、不覆盖新的未知内容。发布后生产reader验证成功才清除意图，恢复资产退出正常resolver。备份仍为恢复资产，不是第二份正式数据库。

已取得的实证：

- 四份实际来源的一致性只读备份经过完整prepare流程，最终185个Session与生产目录185项一致，保留33,593个Event、217个Run、563个receipt、175个named snapshot；各类Artifact经typed reader与已支持的State/Event引用校验。140个Session保持恢复要求，未重派旧任务。历史正常入口最终为零。该演练使用全新私有测试目录，并非本机原文件处置。
- 生产Fork和删除tombstone fixture验证关联与不复活规则；实际四源没有Run Fork origin，不能用真实数据零值替代该fixture。
- 发布测试覆盖意图半写、目录同步、主文件及WAL/SHM移动中断、候选变更、发布后reader拒绝与重启接续；阶段记录损坏或来源出现新内容时不覆盖。
- 真正source CLI在隔离home自动转换生产schema9空历史库后正常返回状态，旧入口撤下。已有真实schema10旧/新版本往返续聊资格继续适用。

发行准入仍必须单独验证。macOS首批source CLI/TUI使用当前build身份、精确父进程身份、标准发行入口检查及旧进程观测，并与Store锁组合；进程观测本身不是未来启动门闩。Desktop配对Service准入与转换后的真实客户端续聊已通过macOS资格；最终门禁与其他发行组合仍分别核对。显式daemon、任意历史安装版和Windows不得由这些证据推定兼容。

尚待完成：未参与新维护协议的旧发行版在正常安装/回滚入口的完整准入约束、更多平台与发行组合的资格，以及最终验收矩阵的缺项。自动准备失败仍需更细地区分空间、发行准入、格式和发布恢复条件；保护性拒绝不能计为自动兼容成功。


### 17.1 本机实际收敛及客户端复核

在隔离四源全链演练、生产客户端9/11续聊、真实packaged Electron自动11转换及两轮历史重启测试通过后，已通过正常source CLI启动流程处理原用户数据。Service退出码为0，执行状态查询成功；未发送新任务或重派旧任务。

最终唯一正常Store为既有正式位置的 `kite-session.sqlite`。三个旧数据库入口已撤下，四份独立验证的一致性备份以及原main/WAL/SHM归档保留在私有 `session-store-recovery`，不参与正常查询。没有待结清发布意图。

迁移前后原Session ID集合摘要完全相等：185个Session、33,593个Event、217个Run、563个receipt、175个named snapshot全部保留。新Store schema10，quick_check与foreign_key_check通过。另经正式Service与RuntimeClient只读分页列出全部185个原ID，并逐一成功打开全部185条历史；共15,912个客户端展示事件，底层Event数仍为33,593（两者为不同投影口径）。140个Session保留恢复要求。

客户端资格：9与11的脱敏fixture分别经真实Service启动准备、原ID打开和续聊、重启读取两轮回复；生产准入与fixture测试分别声明。真实packaged Electron另验证精确11历史profile自动整理、原ID和回复保留、不重放模型、不兼容错误展示，五次启动均正常退出。TUI startup与session-persistence PTY复验通过。全18工作区类型检查、核心/API边界、修正后的runtime包边界、测试归属、文档结构/影响及计划证据均已通过各自检查；完整最终门禁仍须以最后修改版本记录。

### 17.2 V1验收矩阵当前状态

| 条目 | 当前证据 | 未完成边界 |
| --- | --- | --- |
| C01 唯一入口 | 当前source/installed解析与Desktop正式入口一致；新用户及现有库启动测试 | 不向任意历史二进制追溯承诺 |
| C02 历史源自动整理 | source CLI/TUI、paired Desktop及installed CLI/TUI已知路径；source daemon由CLI一次性stdio先准备；本机四源收敛 | 精确来源及入口分别资格，不宣称任意未知格式兼容 |
| C03 升级保全 | 真实旧/新10往返；9/11精确子集fixture及实际四源 | 不是任意历史版本矩阵 |
| C04 原ID续聊 | 9/11 RuntimeClient与packaged Desktop原ID续聊及重启 | 已验证路径通过 |
| C05 关系与执行事实 | Fork/tombstone生产fixture、Artifact引用、实际185会话；140仍需恢复 | 未来State/Event字段须补验证器 |
| C06 失败与中断 | 备份失败、空间不足、事务回滚、意图半写、main/WAL/SHM发布边界恢复 | Windows文件发布未资格 |
| C07 解码失败可见 | Store坏State/Event/引用拒绝；TUI第二页失败不再吞为空列表；Desktop局部错误保留旧目录 | 已验证读取路径通过，不对无法确认授权的坏元数据暴露内容 |
| C08 降级再升级 | 同10真实旧/新往返；当前安装器持有选版锁，已声明契约根拒选无契约候选；数据不覆盖 | 任意旧安装器或手动直启二进制不追溯受控 |
| C09 并发与维护 | POSIX维护锁与崩溃释放；installed发行租约覆盖发布，installer共用稳定锁，卸载重装不换锁inode | 任意旧非参与入口及Windows未资格 |
| C10 Workspace与授权 | Desktop与真实Service测试：Workspace失效或被重定向仍可读历史，执行拒绝；关联逐列保留 | 已验证路径通过，不自动重新授权或重写身份 |
| C11 未知格式 | exact preflight拒绝、字节保留、Desktop明确诊断 | 自动选择兼容程序与恢复交互尚有边界 |
| C12 本机四源 | 已完成；185原ID与所有历史逐一打开 | 无本机数据收敛待办 |
| C13 取消与重启 | 3项真实子进程测试：共享owner阻止维护、取消后重试、发布崩溃重入、竞争者busy与单次resume | POSIX参与协议的进程通过；Windows及旧非参与程序不在此证据范围 |
| C14 未持久化内容 | Desktop 51项UI测试含切换/失败/恢复时草稿保留；文档明确未发送输入/未落盘片段不属Store保证 | 不承诺草稿跨退出、刷新或升级自动保存 |

因此目前可确认本机数据连续性与已列路径启动/续聊；不能把整张V1矩阵宣布全部完成。

### 17.3 后续边界修复与验证

TUI历史目录第二页失败此前会被catch转成空列表，现传递至既有局部错误展示；回归测试通过。Desktop隔离Service测试夹具改用严格限于临时目录的入口，不冒充source CLI/TUI生产身份；历史及Workspace导航测试2项、141断言通过，UI测试51项、350断言通过。

磁盘空间预检及准备中的ENOSPC/EDQUOT/SQLITE_FULL使用封闭的 `store_insufficient_space` 诊断，客户端给出释放空间后重试的操作提示，并提醒保留会话及恢复资产。较旧schema数字本身不再被标为存在可用转换；精确已知来源的转换仍由准备器单独决定。

准备器支持持有式发行准入租约，覆盖备份、候选验证、发布或中断续传，发布前复核并在finally释放；生命周期成功与失败路径测试通过。安装版接线和发行选择协议尚在最终资格验证，不能由该函数测试推定整条安装路径通过。

daemon无运行实例时交由Service准备或明确拒绝，只向客户端传递白名单有界启动诊断；已有实例restart继续完整预检Store、历史源和待发布意图，失败不先停止旧实例。8项测试通过。run-daemon身份准入仍未资格，已知11测试验证拒绝与字节保留，不代表daemon自动迁移成功。

### 17.4 安装版准入与最终客户端复验

macOS安装版CLI/TUI现通过稳定launcher、候选manifest摘要、Service摘要及PID/启动身份验证取得持有式发行选择租约。当前安装器在同一安装根锁下install/rollback/uninstall；已声明维护契约的安装根拒绝选择未声明该契约的旧候选。该约束不追溯约束任意旧checkout安装器或手动直启旧二进制。

候选 `0809bc1b2df42aa5c82522cc` 在隔离临时home/prefix中经稳定CLI与真实TUI PTY启动，分别将schema9空旧库自动整理为schema10，旧入口撤下，无待发布意图；这是入口准入资格，含历史内容的保全另由第17.1节和非空fixture证明。发行/安装/锁定向20项、204断言通过。随后的卸载锁inode与维护标记中断恢复修正须以其最终测试为准。

同一候选的真实packaged Desktop最后复验通过五次启动：新库、当前历史重启、精确11格式转换后原会话续聊、续聊后重启以及不兼容只读拒绝，五次均正常退出。最后一组准入/精确格式/维护取消/TUI分页18项、84断言通过。全18工作区类型、runtime包边界、核心/API边界、测试归属与文档结构检查已通过；最终格式检查正在随最后owner修正收敛。

### 17.5 daemon自动准备与最终P1审查

首次daemon启动现由CLI复用已资格的短暂run-stdio Service，仅初始化以执行Store准备；等待该Service完整关闭，再查询daemon状态，只有仍absent才启动常驻进程。准备失败不spawn，已有实例restart的保护性预检仍保留。真实source CLI隔离测试验证9/精确11自动prepare、daemon ready及stop，Session/Run均未新增；未知12拒绝、不启动daemon且源文件不变。安装版同流程的最终候选资格另记录。

最终只读P1复核确认卸载/重装双inode竞态已关闭：卸载保留安装根、稳定锁及已有维护契约；重装须取得同一锁并遵守旧候选限制。维护标记在exclusive锁下用固定scratch完整写入/fsync后原子发布，磁盘满与半写scratch下次重试通过；scratch只在exclusive下回收。修正后12项、78断言通过，无已发现的剩余P1。

## 18. 当前交付状态（最终候选验收后）

本节为当前状态，覆盖前文阶段性记录。之前代码已在持久分支 `refactor/remove-session-global-gates` 提交为 `47c5e4f16c6675cb99712a696abd8a5e485abe27`。本轮兼容性修改仍未提交，目标保持进行中；完整V1尚不能宣布全部完成。

### 已完成并验证

- 本机四份历史来源收敛至原正式 `kite-session.sqlite`；迁移前后185个Session ID集合相同，33,593条Event、217个Run、563条receipt、175个named snapshot保留，全部185条历史经正式Service与RuntimeClient逐一打开。140个Session保留真实恢复要求，没有重派旧任务。
- macOS source与installed CLI/TUI、paired Desktop，以及由CLI发起的首次daemon启动，已有已知格式自动准备路径；daemon复用短暂stdio Service准备，初始化不创建Session/Run，完整关闭后才进入daemon启动。
- 对明确已知的9/11子集转换与多份10合并进行内容、关系和执行事实验证；未知语义不被丢弃，不靠改schema数字放行。备份、候选与原文件归档均退出正常数据入口。
- 持续持有Store维护锁；installed同时持有发行选版租约。当前安装器协作选版，已声明契约的根拒选无契约旧候选。卸载保留稳定锁，防止重装获得另一inode；维护标记半写及空间不足可以重试。
- TUI历史分页不再吞错返回空列表；Workspace失效/重定向时保留历史可读并拒绝不合规执行；草稿按现有进程内承诺验证，不冒充数据库内持久内容。

### 最终产物与证据

最终本地构建候选：`8bf2919626c87bbb388bf6c9`，归档摘要 `sha256:7d484f4825ab551f04dc669e6bc3798f046b10cdb3b278a95b8d4032c9c64449`。未发布。

| 验证 | 结果与范围 |
| --- | --- |
| 安装版可复现资格 | `installed-store-startup.test.ts`，显式archive输入：2项、38断言。CLI9、真实PTY TUI9、daemon9/11自动准备与启停通过；均0新增Session/Run。使用私有隔离HOME及允许macOS原生身份检查的环境。 |
| 最终配套Desktop | 同一候选五次真实packaged Electron启动通过：新库、原历史重启、精确11转换后原ID续聊、续聊后重启、不兼容拒绝；均正常退出。 |
| source daemon | 真实source CLI 9/11准备→ready→stop通过，0新增Session/Run；未知12拒绝、不启动daemon、原字节不变。9项daemon定向测试含竞争启动与existing restart保护通过。 |
| 维护/发行故障 | 发行锁、卸载重装、ENOSPC标记与半写恢复12项、78断言通过；真实子进程维护取消、崩溃与竞争恢复通过。 |
| 历史/Workspace/草稿 | Desktop历史及导航2项、141断言；UI 51项、350断言；TUI分页错误回归通过。非空9/11转换后原ID续聊及真实旧/新10往返资格已通过。 |
| 代码与文档门禁 | 18工作区类型检查、格式检查（仅既有提示）、runtime/core/API边界、测试归属、文档结构/影响及计划门禁通过；最终diff无空白错误。 |

安装版daemon初次资格曾在受限工具沙箱中因 `/bin/ps` 被禁而失败。随后在允许该必要原生检查的环境下，用全新9/11隔离库完成全链验收；没有为通过测试放宽产品身份核验。空库入口资格与非空历史保全证据分别声明，不能互相替代。

### 尚未完成的方案范围

1. 第9节的启动恢复交互尚未完全交付：当前有安全错误码、格式信息、磁盘空间说明与重试，但仍缺完整的迁移阶段/取消限制展示、权限与损坏细分诊断，以及脱敏诊断保存出口。普通连接关闭已不再遮蔽已识别Store诊断，但不声称所有启动失败均已分类。
2. Windows原生维护、文件发布和跨客户端版本资格未运行。Linux具备部分POSIX原语，不据此宣布其自动迁移入口已取得资格。
3. 当前发行协议约束参与协议的安装器与受控入口，不能追溯约束任意旧checkout安装器、手工复制或直接执行的历史二进制。未知格式、未经证明的11语义和不合规发行入口仍会保留数据并停止准备；这些保护性拒绝不是兼容成功。

因此，本机实际数据连续性和上述macOS受支持路径已取得实证；完整V1仍为进行中，尚未进入“所有方案项完成后提交”的状态。

## 19. 启动阶段与脱敏诊断补齐（当前状态）

本节更新第18节启动交互的实施状态；完整V1仍在进行中，兼容性修改仍未提交。

Service现在分别输出严格白名单的准备阶段和终止诊断。阶段为检查数据、取得维护权、准备与核对、发布、就绪；Desktop启动界面轮询独立的Host状态，CLI/TUI显示受限阶段说明。阶段消息不占用终止诊断窗口，初始化完成后忽略启动阶段。任意stderr、超长行、额外字段和未知阶段不作为产品诊断展示。

已细分权限/访问失败、数据库损坏或当前结构完整性失败、空间不足、维护占用与不支持格式。诊断包含已验证错误码、实际/目标schema和阶段；不能从较旧schema数字推导可迁移，也不把未知格式当作可修复损坏。

Desktop已有“保存诊断”原生文件对话框；只写白名单报告，取消不写入，以独占新建和0600权限保存，已有文件不覆盖，renderer不提交任意路径或报告。CLI/TUI在终止失败时输出相同白名单报告，可复制或重定向保存。报告不包含原始stderr、目录、数据库内容、会话正文或凭据。

### 本阶段证据

- 新候选 `188e7635f2461bd55c4eae3b` 已构建且未发布。真实packaged Desktop五次启动通过：新库、当前历史重启、已知11转换后原ID续聊、续聊后重启、不兼容拒绝。新增断言确认不兼容时“保存诊断”入口实际可见，原数据库字节不变。
- 启动编码、stdio传输、真实Service进程和TUI诊断集成28项、161断言通过；Desktop Host/Service/桥接/UI/诊断保存定向70项通过。
- 存储权限/损坏/空间分类和阶段回调17项、79断言通过；失败保留原文件。维护与发布故障10项、125断言通过。
- TypeScript根检查与全部18工作区检查通过；runtime/core边界、测试归属、文档结构和发布计划检查通过。安装版同一候选2项、38断言通过，覆盖真实CLI9、PTY TUI9与daemon9/11自动准备和启停。归档SHA256为 `bb08574ef05c62c88ad0cdc7190a07272a6a41169e19e4c324725c561a4edc27`。

### 保留的未完成项

第9节要求的“提交前可安全取消、提交期间等待结算”尚未交付。当前阶段说明提醒等待，未提供虚假的安全取消按钮。终止Service后依靠发布意图重入属于崩溃恢复，不能以此证明安全取消；后续实现必须让取消与发布边界有确定顺序，并让客户端等待发布结算。

第18节的Windows/Linux资格和任意旧二进制边界继续有效。本阶段不把保护性拒绝标作兼容成功，也不新增正式数据库或自动回退空库。

## 20. 安全退出与发布前取消（当前状态）

本节更新第19节的取消实施状态。目标仍进行中；本轮修改仍未提交。

准备器现在异步等待发布前决定。候选生成及验证完成后，维护锁和写入者准入继续持有；取消返回 `store_preparation_cancelled`，不写发布意图，原数据保持不变。允许提交后重新核验准入及源文件指纹，再同步发布。已有持久发布意图的启动恢复不接受取消，必须先完成结算。

stdio Service在准备前接管SIGINT/SIGTERM，把信号记录为退出请求；在发布前安全点让事件循环处理已到达的请求，再作取消或提交决定。请求与提交相邻时可能由提交先取得顺序，此时继续完成发布并退出，不能声称取消成功。初始化carrier安装信号监听后才移交启动监听，避免接管空隙。当前格式无迁移时也处理启动期退出请求。

Desktop通过现有应用退出动作请求停止启动中的自有Service，等待真实退出；Host在等待或失败时保留进程所有权。CLI/TUI与daemon的短暂准备连接显式接入父进程启动期SIGINT/SIGTERM监听，初始化完成后移除。预初始化关闭不再使用计时SIGKILL打断发布；已初始化后的普通运行关闭仍沿既有有界收尾。窗口关闭隐藏与应用退出保持原产品语义，不新增含糊的“立即取消”按钮。

### 验证与边界

- 存储、传输、诊断与真实Service资格整合35项、198断言通过。覆盖取消时维护锁持有、原字节保留、无发布意图、提交后重验，以及已提交意图绕过取消继续恢复。
- 四项真实Service子进程资格分别覆盖发布前信号取消、发布期间信号后完成结算、当前格式就绪退出、已有发布意图恢复期间收到信号。失败使用生产诊断编码/解析，成功退出不伪报取消。
- Service连接及任务重启定向10项、89断言通过。全部18工作区类型检查通过。
- 新本地候选 `543b60a0ef39745b4ace25f3`，归档SHA256 `6cdb0187b3484582ae0c7126dd11ee767d785ab770ae170ef9d69cac7095338c`，未发布。安装版CLI、真实PTY TUI及daemon正常准备启停2项、38断言通过。
- Desktop进程/Host关闭20项定向测试通过，包含超过旧15秒期限继续等待，以及启动失败后超过旧2秒期限继续结算。新候选真实packaged Desktop五次启动全部通过，覆盖新库、历史重启、精确11转换后原ID续聊、续聊重启与不兼容诊断导出入口。文档结构/全量影响、runtime/core边界、测试归属、计划证据与格式门禁通过（格式仅8项既有提示），diff无空白错误。

这些证据覆盖本机POSIX信号和受控自有Service，不把Windows的进程终止语义当作同等安全取消。完整跨平台资格和完整方案审查仍未完成；未授权/未知语义格式继续保留数据并停止准备。客户端请求之后的强制操作系统杀进程仍属于崩溃恢复范围。

## 21. 逐项完成审查与补充客户端资格（当前状态）

本节为当前状态，覆盖第20节及以前的阶段性完成表述。目标仍进行中，修改尚未提交。完成审查发现，分层锁/Service测试和正常启动烟测不能代替全部客户端矩阵，因此补充了以下直接证据：

| 补充场景 | 当前结果 | 仍需区分的边界 |
| --- | --- | --- |
| 非空Store11在真实TUI中恢复、续聊 | `session-persistence.test.ts` 现在分别运行Store10重启和精确Store11转换，两场景37断言通过；真实PTY使用 `/resume` 选择历史，原问答可见，后续消息落在原Session ID | 这是精确已知11子集的fixture，不代表任意历史11；非空9在真实TUI中的同类矩阵尚需补齐 |
| packaged Desktop准备期间退出 | `startup-store-smoke.ts`在真实UI显示preparing后发起应用退出及既有确认，Electron正常退出，原Store11哈希不变、无发布意图；重启后自动整理并打开原ID和两条历史，整个脚本exit0 | 退出链以磁盘结算及重启结果为证，不伪称收到了Service取消诊断；真实TUI准备期间Ctrl+C仍待验 |
| 不兼容受控回滚后保留新增会话 | 真实旧候选 `86a5148e07c4c880efd06169`（旧提交47c5e4f1）与当前 `543b60a0ef39745b4ace25f3`：旧版写N，当前版实际完成9来源收敛后新增M；正常installer拒选无维护协议旧制品，指针和Store哈希不变；当前版重开按原ID核对N+M完整问答 | 这是维护协议不兼容的受控回滚。仅同schema10、没有发生维护时允许兼容旧版回退，不把正常兼容误标为应拒绝 |
| 已安装Service/CLI混合版本并发 | 同一脚本保持真实旧Service连接；新CLI先拒绝迁移且原库及历史源不变；旧Service仍完成一轮；旧退出后同一新CLI整理成功；后续回滚拒选通过 | 不能替代不同版本Desktop/TUI同时打开的客户端资格 |
| Linux存储原语 | 在隔离Linux x64容器、Bun1.4.0下运行维护锁、进程崩溃恢复和启动取消三组测试，12通过、1仅Windows负向测试跳过、0失败 | Apple Silicon上的x64容器环境；不是CI Bun1.4.2或Linux产品自动迁移入口资格。Linux原生依赖在隔离副本安装，宿主依赖未改；容器与副本已清理 |
| 跨平台CI补齐 | 现有原生矩阵新增macOS/Linux三组资格；Windows新增通过真实ACL接口的LockFileEx共享/独占、崩溃释放和文件身份测试，并要求Windows平台实际执行 | 未推送、未触发远端CI；Windows代码仍无原生通过结果，不能以本机skip认定完成 |

`session-store-incompatible-rollback.ts`接受两个真实候选archive，可复现并发拒绝、旧writer继续健康运行、旧退出后准备、新增内容保留及受控回滚拒选。旧归档由独立旧源码与其依赖构建，未修改旧代码；本轮所有客户端资格使用私有测试HOME，未再次迁移用户实际数据。

资产审计确认：受支持AppServer路径把八类Artifact和文件前像内容写入数据库后端。保留清单及连续性验证覆盖这些表和已知State/Event/命名快照引用；不存在为本轮路径承诺的独立附件表。外部工作区文件、配置、技能、日志和正文中任意路径不自动导入；数据库备份不代表它们已备份。已知Artifact引用缺失或损坏仍拒绝发布，不静默跳过。

### 完成条件仍未全部满足

- C07的真实TUI分页失败展示、非空Store9的TUI原ID续聊，以及TUI准备期间Ctrl+C仍缺直接客户端资格。
- C09已有Service/CLI并发实证，Desktop/TUI不同版本同时运行的场景尚未完成。
- Windows维护与发布资格、CI使用Bun1.4.2的Linux结果仍缺；Linux自动迁移准入仍有明确平台限制。不能用本轮Linux存储原语结果宣布产品入口支持。
- 完整最终review须在上述必要范围收敛后执行；本阶段审查、单项通过或保护性拒绝均不等于完整V1完成。

本机已有185条会话收敛与历史逐一打开的既有证据保持有效，本轮没有重复操作这些用户数据。

## 22. 客户端矩阵补齐与并发 P1 修复（当前状态）

本节覆盖第21节及以前的当前状态说明。之前代码已提交；本方案修改仍未提交、未推送。目标保持进行中，Windows原生资格和完整提交前审查尚未闭合。

### 本轮发现与修复

- **发布检查与开库之间的竞态。** 另一进程写入发布意图并归档旧源后崩溃，锁外初检可能把缺失canonical误认成新库。已用故障注入复现修前普通owner成功创建空库。现在普通owner取得共享维护锁后复查意图与历史来源，再开库；准备器取得独占锁后重新读取意图，按确切锁清单接续已提交发布。锁清单变更时返回可重试的busy，不另开一次迁移。
- **TUI历史错误导致进程退出。** 移除历史分页吞错后，损坏库中的启动准入失败会拒绝尚无等待者的readiness Promise，Bun因此退出。创建Promise时即观察拒绝，后续等待者仍收到原错误；没有全局吞错处理。真实TUI已显示第二页读取错误，保留全部原会话ID与故障记录。
- **自定义安装根旧进程漏识别。** 正式安装器支持自定义prefix，Desktop不能只识别默认根。现对同用户、符合固定安装布局的实际Kite可执行进程保守判busy；路径形状不授予父进程豁免或安全执行资格。仍不声称能够追溯围栏任意未参与协议的旧launcher未来启动。
- **Windows普通启动的检查边界。** 只读意图检查此前无Windows权限对象却进入发布目录ACL路径。现无意图时允许继续进入已有ACL校验与共享维护锁；有意图时仍拒绝。未启用未经资格的Windows自动迁移；新增Windows测试在macOS只能跳过，不能计作原生通过。

### 新取得的直接证据

| 场景 | 结果及范围 |
| --- | --- |
| 非空Store9/10/11真实TUI恢复与续聊 | 三个独立PTY旅程均在原Session ID追加后续消息，原问答可见；转换夹具来自实际TUI已写业务行及严格已知旧结构，不是只改schema数字 |
| TUI准备期间Ctrl+C | 真实PTY在准备进度可见后发送Ctrl+C，退出后无待发布意图；未发布时原源哈希不变，提交先发生时验证已完成结果；重启仍可打开原会话，不伪称所有时序都取消成功 |
| C07第二页故障 | 真实Service创建102个会话；生产目录第一页100条成功、第二页失败；TUI显示明确错误、不显示成功空列表，全部102个原ID与损坏字节保留。1项221断言；完整TUI facade回归26项103断言 |
| C09不同版本Desktop/TUI | 旧候选86a5148e07c4c880efd06169的安装版TUI与当前packaged Desktop共享私有HOME。旧writer存活时Desktop拒绝、原Store9/10字节不变；旧TUI仍完成实际模型turn；旧退出后Desktop完成归并并读取同一Session/Run/events，无模型重放。1项24断言；自定义prefix真实进程识别3项22断言 |
| 发布竞态P1 | 修前故障注入复现；修后共享owner拒绝pending/未归并来源，独占准备重读后来出现的intent并接续。独立存储复验24项204断言通过 |
| Linux Bun1.4.2 | 官方Linux x64运行时1.4.2+744846f84，在Apple Silicon Docker模拟环境完整运行六文件：维护锁、进程中断、启动取消、发布、Runtime Storage和准备，共46通过、1项Windows专属跳过、0失败，307断言。原始stdout保留；首次隔离副本漏拷源码的问题修正后完整重跑，无宿主依赖修改 |

本轮候选为 `e1112805c77cfdf3bb10b32c`，归档SHA256 `783c3c22c02d68e02c77a1385398b60dbc51b336bdfac16e6f4c84abb70c65a7`。已重新构建配套Desktop；真实packaged启动脚本通过新库、历史重启、精确11转换后原ID续聊、准备期间应用退出后重启、未知格式明确拒绝及诊断保存入口。以上新客户端资格均使用隔离测试HOME，未再次修改本机185条既有会话。

最终候选的安装版CLI、真实PTY TUI与daemon准备/启停另有2项38断言通过；最终source TUI三个隔离scenario文件共5个旅程、285断言通过，覆盖非空9/10/11、准备期Ctrl+C及第二页故障。全部18工作区与根TypeScript检查、core/runtime/API边界、测试归属、文档结构/全量影响、计划证据及格式检查通过，格式仅保留既有提示。

### 尚未完成

Windows原生维护锁、普通开库及文件发布资格没有实际runner结果；Linux已有上述存储层执行证据，但生产自动迁移准入仍有限制，不能把容器测试当成完整Linux客户端支持。任意旧非参与二进制的边界保持原声明，不以当前测试扩大承诺。完整方案仍需完成剩余平台验收与最终整体提交前review，不能将本轮P1修复和macOS客户端通过表述为完整V1完成。

## 23. 自动等待补齐与整体代码审查（当前状态）

本节为当前交付状态，覆盖第22节及以前的阶段性结论。目标仍进行中；兼容性改动未提交、未推送。

逐项核对第5、9节发现：此前的busy保护只有Desktop手动重试及TUI重启，不能证明“竞争者结束后当前客户端自动继续”。现在由现有Service启动组合处理明确的 `store_busy`：每250ms重新执行准备与开库，从首次busy起使用单调时钟分配10秒等待预算。每次失败都释放本次维护资源，正常启动无额外等待；超时仍返回带 `waiting_for_store` 阶段的busy。只有owner实际打开成功后报告ready。

已确认的旧进程占用可以进入此等待；身份不明、进程检查不完整、格式/权限/损坏错误保持明确拒绝。安装发行选择锁目前把忙与不安全情况合并为非类型化错误，因此该类别不猜测为可重试busy。等待提示说明现有客户端或整理流程占用，不编造PID或具体实例身份。

stdio启动退出请求可停止等待；发布已经完成后收到退出请求，沿原owner收尾，不伪报提交前取消。重试成功后清除上一轮busy事实，避免“先忙、后发布成功、再退出”仍报过期错误。没有增加第二份数据库入口、后台协调服务、配置开关或通用重试框架。

### 最终集成证据

| 验证范围 | 结果 |
| --- | --- |
| Service真实维护锁等待 | 4项通过：锁释放后自动打开、10秒超时无空库、等待中取消无残留、先busy后完成发布再退出不报告过期busy或提交前取消 |
| 阶段协议与客户端显示 | 61项、428断言通过；Desktop/TUI显示自动重试与退出说明，封闭诊断仍拒绝未知阶段与额外字段 |
| 真实TUI自动继续与取消 | 2项、14断言通过：同一PTY在独立holder释放后直接ready并打开原ID；Ctrl+C不终止独立holder，释放后重启原历史仍在 |
| C09真实混合版本客户端 | 最终制品1项、22断言通过：旧安装版TUI仍可写入时新Desktop等待，旧TUI完成一轮后退出；按精确PID/start identity确认旧writer结束，同一Desktop无需重启即可整理并读取原Session/Run/events，模型请求未重放 |
| C08升级新增与回滚保护 | 旧候选86a5148e07c4c880efd06169到当前候选的真实CLI/Service资格通过：旧writer存活时保护原Store，退出后整理；新增M后拒选不支持维护契约的旧候选，再开当前程序保留原N+M与全部问答 |
| 每个源文件发布边界 | 原有fault测试改为精确阶段匹配，覆盖两源main/WAL/SHM逐个rename、源目录fsync、归档目录fsync及候选/意图边界；10项167断言通过 |
| Linux最终变更 | 官方Bun1.4.2 Linux x64隔离容器，Service等待、启动取消和发布故障三组共18项203断言通过；原始输出保留，隔离副本及容器已清理。上轮未变的维护锁/合并等证据继续有效 |

最终本地候选 `2e94aca08bb6daebfc9f556c`，归档SHA256 `33e1c91da16daef76ceebba5ccbea5329a3ea7f511d2fac7489eeb1e5cfd8987`；Desktop配套Service已重新构建。原生测试均使用私有测试HOME，没有再次迁移或改写用户实际会话。

最终候选的packaged Desktop完整启动脚本再次通过；安装版CLI/TUI/daemon准备与启停2项38断言通过。最终source TUI四个隔离scenario文件共7个旅程299断言全部通过。根与全部18工作区类型、core/runtime/API边界、测试归属、格式、文档结构/全量影响和计划证据门禁通过；格式仍仅8项既有提示，diff无空白错误。

### 审查与未完成边界

本轮对完整存储、发行准入/安装回滚、Desktop/TUI/stdio生命周期与诊断差异做了分区只读审查，并由主任务复核新增等待边界。未发现新的可证P1；这不能替代尚未运行的平台资格。既有P2和已声明的任意旧非参与launcher边界未扩展修复。

Windows原生维护锁、普通Store打开与文件发布资格仍缺实际执行环境；已提出执行环境信息请求，未将本机skip作为通过。Linux存储资格也不代表已开启其所有产品迁移入口。现有本地实现和客户端证据不能据此宣布完整V1完成；取得必要平台结果后还须核对最终交付证据。

## 24. 最终交付与验收范围确认

用户于2026-09-17明确指示“Windows 验收先不管”。据此，Windows原生执行不再阻塞本次交付；仅暂缓该项验收，不删除测试、不启用未经验证的迁移路径、不把Windows标为通过。本节覆盖前述“仍进行中”及“等待Windows才能完成”的阶段性结论。

**本次约定范围已完成。** 唯一正式入口、受支持格式转换与历史来源收敛、维护准入、备份及可恢复发布、会话保留校验、原ID历史读取与续聊、真实客户端自动等待与退出、启动诊断、受控回滚保留新增内容均已实施。第23节最终制品和直接客户端结果保持有效；本次仅更新验收范围与交付记录，没有修改产品代码或重新迁移用户数据。

完成核对再次读取了最终C08/C09、packaged Desktop、安装版CLI/TUI/daemon、source TUI、Linux变更测试及类型/边界/文档门禁的原始日志：相关测试通过；根及18工作区类型检查通过；整体分区审查与主任务复核未发现新的可证P1。原有P2按用户指示不纳入本次修复。

交付限制保持明确：schema11只支持已证明的精确子集；未知格式、冲突及未经证明的执行语义不强制转换；历史读取不等于旧Run无条件续跑。Linux存储层容器资格不代表所有Linux产品迁移入口已启用。Windows原生维护锁、普通打开和发布资格未验收。任意绕过受控入口的旧launcher不获得追溯并发保护。备份、候选和归档为恢复资产，不是第二个正常读写数据库。

此前代码提交为47c5e4f1。本次兼容性修改仍在持久工作树中，未提交、未推送；不再为Windows CI请求发布代码。完成状态针对实施和当前约定验收，不表示跨平台发布或Git提交已完成。

## 25. 真实旧会话执行权阻断修正（2026-09-17）

此前完成审查漏掉了 State／Run 已完成但独立执行权仍为 recovery_required 的真实历史路径。派发前已被拒绝且已耗尽的工具记录不代表存在未收尾执行。发送时通过原 writer 事务核对完整终态、无 owner／lease、无 active／unknown Run 及无 prepared／unknown effect，满足条件才确认遗留 cleanup 并取得执行权；保留原事件、旧 Run 和失败记录。

实际用户 Store 的只读备份在隔离副本通过 DesktopClient→Service 与本地模拟模型验证：原会话直接新轮发送完成，299 条历史事件哈希不变，旧 Run 保持 completed，185 个会话数量不变，新回复写回原 sessionId。该验证没有修改用户原 Store，也不代表外部模型联网验收。此项补充替代此前对该路径不充分的完成判断。
